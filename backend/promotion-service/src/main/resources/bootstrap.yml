spring:
  application:
    name: promotion-service
  profiles:
    active: local
  cloud:
    nacos:
      server-addr: 192.168.163.129:8848
      config:
        file-extension: properties
        shared-configs: # 共享配置
          - data-id: shared-jdbc.properties
          - data-id: shared-jwt.properties
          - data-id: shared-redis.properties